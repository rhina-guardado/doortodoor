<div class="flex flex-col gap-2 w-90 max-w-sm p-4 mx-auto pb-1">
   <label for="{{id}}" class="font-semibold">
      {{label}}
   </label>
   <input id="{{id}}" type="{{type}}" name="{{name}}" placeholder="{{placeholder}}" class="rounded-lg border py-1.5"/>
</div>